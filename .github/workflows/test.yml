name: Deploy Function

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      SUPABASE_ACCESS_TOKEN: sbp_4b1b28aceaf5ca56e377a521542d4f1dc2e53a98
      PROJECT_ID: cveicdaizlxtslvosxqm
      SUPABASE_DB_PASSWORD: gYPndVdJQlupiJoS

    steps:
      - uses: actions/checkout@v3

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - run: supabase link --project-ref $PROJECT_ID
      - run: supabase backend deploy
